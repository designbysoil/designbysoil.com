$(document).ready(function() {

	// Slick Slider
	$('.slideshow').slick({
		infinite: true,
		slidesToShow: 1,
		autoplay: true,
		autoplaySpeed: 3000,
		dots: false,
		prevArrow: false,
		nextArrow: false,
		fade: true,
  		cssEase: 'linear'
	});

});